import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  floatingButton: {
    position: 'absolute',
    zIndex: 80,
    bottom: 0,
    alignSelf: 'center',
  },
});
